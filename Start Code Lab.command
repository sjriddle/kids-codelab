#!/bin/bash
# Double-click this file to open the Code Lab — no typing needed.
# (macOS opens .command files in the Terminal.)
cd "$(dirname "$0")" || exit 1
echo "Starting the Code Lab... (close this window or press Ctrl+C when you're done)"
python3 code_lab/code_lab.py
